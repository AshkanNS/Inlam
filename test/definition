def abde(min):
    if min <= 33:
        return "Kontant"
    elif min <=66:
        return "Normal"
    else:
        return "Plus"

a = int(input ("Hur många minuter ringer du per månad? "))
print (abde(a))


